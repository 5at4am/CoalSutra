"""End-to-end pipeline orchestration for CoalSutra (CMPDI Reporting Assistant).

This module is intentionally a stub at scaffold stage. It will grow into the
shared ingestion backbone that drives all three modules:

    ingest -> route -> extract -> normalize -> validate -> store
        -> (query | report | topics)
"""