# Set for local dev only. On Vercel the frontend + backend share one domain,
# so leave this unset and the app calls /api/v1/* same-origin via vercel.json rewrites.
NEXT_PUBLIC_API_URL=http://localhost:8000